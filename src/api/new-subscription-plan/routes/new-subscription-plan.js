'use strict';

/**
 * new-subscription-plan router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::new-subscription-plan.new-subscription-plan');
