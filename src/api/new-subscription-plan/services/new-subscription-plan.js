'use strict';

/**
 * new-subscription-plan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::new-subscription-plan.new-subscription-plan');
